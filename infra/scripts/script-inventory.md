# Scripts

Helper scripts for cluster checks and experiment setup. These should stay small, readable, and safe to run from my workstation.

## Current Scripts

- `check-cluster-readiness.sh`: read-only SSH checks for both DGX Spark nodes after power-on.
